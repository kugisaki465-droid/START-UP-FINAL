# Render.com deployment configuration
services:
  - type: web
    name: sakaysmart-butuan
    runtime: node
    rootDir: backend
    buildCommand: "npm install && mkdir -p data && node src/database/seed.js"
    startCommand: "node src/server.js"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: DB_PATH
        value: ./data/sakaysmart.db
      - key: CORS_ORIGIN
        value: "*"
